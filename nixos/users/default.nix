{ config, pkgs, lib, meta, ... }:

{
  # Default user configuration
  # This is used when no specific user configuration exists for the host type
  # You can leave this empty or add default users here
}
